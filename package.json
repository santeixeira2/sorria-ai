{
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "devDependencies": {
    "turbo": "^2.5.0"
  },
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "dev:api": "turbo dev --filter=@sorria/api",
    "dev:mobile": "turbo dev --filter=@sorria/mobile"
  }
}
