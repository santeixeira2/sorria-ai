/** Design system entry — use `useAppTheme()` in UI; this file re-exports building blocks. */
export type { AppColors } from './colors';
export { darkColors, lightColors } from './colors';
export { createTheme, type AppTheme } from './createTheme';
export { radii, space } from './tokens';
